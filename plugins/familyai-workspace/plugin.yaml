name: familyai-workspace
version: "0.3.0"
description: Bootstraps the user-owned ~/AI workspace, file method, and journal without making user data depend on Hermes, and injects a short workspace directive at session start.
